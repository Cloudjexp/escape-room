The theme (backgrounds, chest, lock) is built with CSS gradients, SVG and
emoji so the project works with zero image assets. Drop your own artwork
here (e.g. `background.jpg`, `chest.png`) and reference it from
`src/styles/global.css` or the relevant `.astro` component.
