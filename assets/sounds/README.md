Sound effects in this project are synthesized at runtime with the Web Audio
API (see `js/app.js`) instead of shipping binary audio files. Drop your own
`.mp3`/`.ogg` files here and wire them up with an `<audio>` element if you'd
rather use recorded sound.
